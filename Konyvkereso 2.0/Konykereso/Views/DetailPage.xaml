<Page
    x:Class="Konyvkereso.Views.DetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Konyvkereso.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:c="using:Template10.Controls"
    xmlns:controls="using:Template10.Controls"
    xmlns:vm="using:Konyvkereso.ViewModels"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <vm:DetailPageViewModel x:Name="ViewModel"/>
    </Page.DataContext>

    <Grid Background="LightGray">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <c:PageHeader Grid.Row="0" Content="{Binding Book.title}"/>

        <StackPanel Grid.Row="1">
            <TextBlock Text="{Binding Book.description}" Width="auto"/>
            <TextBlock Text="HEllo ez oitt egy textbox"/>
            <Button>Gomb</Button>
        </StackPanel>
    </Grid>
</Page>
